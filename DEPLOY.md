# 🚀 Быстрый деплой VK бота на Railway

## ✅ Что нужно загрузить в GitHub:

Из папки `vk_ready/` загрузите ВСЕ файлы:

```
✅ vk_bot.py           - код бота
✅ requirements.txt    - зависимости
✅ runtime.txt         - Python 3.11
✅ Procfile            - команда запуска
✅ .gitignore          - защита .env
```

## 📋 Пошаговая инструкция:

### Шаг 1: Создайте репозиторий на GitHub

1. https://github.com/new
2. Название: `vk-beauty-bot`
3. **Приватный!**
4. Без README

### Шаг 2: Загрузите файлы

**Вариант A: Через веб-интерфейс**

1. Откройте созданный репозиторий
2. Нажмите "Upload files"
3. Перетащите все 5 файлов из `vk_ready/`
4. Commit changes

**Вариант B: Через терминал**

```bash
# Перейдите в папку vk_ready/
cd vk_ready

# Инициализируйте git
git init
git add .
git commit -m "VK bot initial commit"

# Подключите GitHub
git remote add origin https://github.com/USERNAME/vk-beauty-bot.git
git branch -M main
git push -u origin main
```

### Шаг 3: Получите токен VK

1. Создайте группу ВК (если ещё не создали)
2. Управление → Работа с API → Создать ключ
3. Права: Управление + Сообщения
4. Скопируйте токен (показывается один раз!)

### Шаг 4: Узнайте свой VK ID

- Откройте свою страницу: `vk.com/id123456789`
- Число после `id` - это ваш ID
- Или: https://regvk.com/id/

### Шаг 5: Деплой на Railway

1. Откройте https://railway.app/new
2. Нажмите "Deploy from GitHub repo"
3. Выберите `vk-beauty-bot`
4. Дождитесь сборки (будет ошибка - это нормально!)

### Шаг 6: Добавьте переменные

В Railway Dashboard → Variables → Add:

```
VK_TOKEN=vk1.a.ваш_токен_здесь
ADMIN_VK_ID=123456789
YOOMONEY_WALLET=4100119468708609
```

**ВАЖНО:** Скопируйте токен ПОЛНОСТЬЮ!

### Шаг 7: Перезапустите

1. Deployments → Latest
2. Нажмите три точки → Redeploy

### Шаг 8: Проверьте логи

В логах должно быть:
```
============================================================
✨ VK БОТ ЗАПУЩЕН ✨
👑 Админ: 123456789
📊 Записей: 0
============================================================
```

## ✅ Готово!

Теперь:
1. Откройте вашу группу ВК
2. Нажмите "Написать сообщение"
3. Отправьте "Начать"
4. Бот ответит! 🎉

---

## 🆘 Если не работает:

### Ошибка "Railpack could not determine how to build"
❌ **Причина:** В репозитории нет нужных файлов
✅ **Решение:** Загрузите все 5 файлов из `vk_ready/`

### Ошибка "Invalid token"
❌ **Причина:** Неправильный токен VK
✅ **Решение:** 
- Создайте новый токен
- Убедитесь что скопировали полностью
- Проверьте что это токен ГРУППЫ, а не пользователя

### Бот не отвечает
❌ **Причина:** Не включены сообщения в группе
✅ **Решение:**
- Управление → Сообщения → Включить "Сообщения сообщества"
- Включить "Возможности для ботов"

### Бот запускается и сразу падает
❌ **Причина:** Не хватает зависимостей
✅ **Решение:** Проверьте что `requirements.txt` загружен

---

## 📊 Структура файлов в GitHub:

После загрузки в репозитории должно быть:

```
vk-beauty-bot/
├── vk_bot.py           (23 KB)
├── requirements.txt    (80 bytes)
├── runtime.txt         (12 bytes)
├── Procfile            (25 bytes)
└── .gitignore          (395 bytes)
```

**Всего: 5 файлов**

---

## 🎯 Чек-лист:

- [ ] Создал группу ВК
- [ ] Получил токен группы
- [ ] Узнал свой VK ID
- [ ] Загрузил все 5 файлов на GitHub
- [ ] Подключил репозиторий к Railway
- [ ] Добавил переменные (VK_TOKEN, ADMIN_VK_ID)
- [ ] Перезапустил деплой
- [ ] Бот отвечает в VK

---

## 💡 Полезно знать:

### Как проверить что бот работает:

```bash
# В Railway логах ищите:
✨ VK БОТ ЗАПУЩЕН ✨
```

### Как остановить/перезапустить:

- Railway Dashboard → Settings → Restart

### Как посмотреть логи:

- Railway Dashboard → Deployments → View Logs

---

**Удачи! Если возникнут вопросы - пишите!** 🚀
